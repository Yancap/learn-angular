import { DiretivaEventoDirective } from './diretiva-evento.directive';

describe('DiretivaEventoDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaEventoDirective();
    expect(directive).toBeTruthy();
  });
});
